export default class UserInfo {
  constructor({ userNameSelector, userDescriptionSelector }) {
    this._userName = userNameSelector;
    this_.userDescription = userDescriptionSelector;
  }

  getUserInfo() {}
}
